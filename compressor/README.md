# 以撒控制台代码压缩器 LuaMin

> **文档基准**：主仓库提交 `275a4f9`（`main`，"更新压缩器文档：补字段前缀折叠、字面量内联、多重赋值拆分三阶段说明，刷新基准 SHA 与测试计数"）。该提交及其之前已包含完整的 compressor 工具、远程模组测试与本文档所述全部技巧。

把"正常编写格式"的 Lua（带缩进/换行/注释/长变量名，可含 `l`/`lua` 控制台前缀）压成**单行、以 `l` 开头**的控制台命令。每一步都用真·Lua 解释器做语法校验、用归一化 AST 做语义等价校验，任一不通过就**拒绝输出并报错**。

> 本文档合并了「压缩器使用/实现说明」与「以撒代码压缩技巧深度剖析」两部分：
> - **第一部分**讲这个自动压缩器：怎么用、做了哪些优化、处理与验证细节。
> - **第二部分**是对人工压缩技巧的系统剖析（成本模型、取舍、变种），既是压缩器的设计依据，也可单独作为手写压缩的参考。

---

# 第一部分　压缩器

## 使用

双击打开 `index.html` 即可：粘贴代码 → 点「压缩」→ 复制结果。纯静态，文件自带 `luaparse.js`（解析器）、`fengari-web.js`（真·Lua 5.3 VM）与 `core.js`（压缩核心），用 `file://` 直接打开就能跑，无需服务器/构建/联网。

界面只显示输入字符数、输出字符数与压缩比；出错时显示拒绝原因。

## 处理流水线

```
预处理(剥 l/lua 前缀, 合并单段)
   │
输入校验（真·Lua load() + luaparse）
   │
阶段1 结构性折叠：局部重命名 + 全局折叠 + 成员折叠 + 仿射因子（统一频次分配最短名）
   │   ├─ 真·Lua 语法校验 + luaparse 校验 + AST 等价校验
阶段1.5 :method 折叠（仅 base 为简单变量；只缩短才提交）
阶段1.6 字段前缀折叠（obj.PREFIX_X 系列共享前缀提取因子；只缩短才提交）
阶段1.65 字符串字面量内联（同标识符样字面量重复出现 → 提取 local 别名；只缩短才提交）
阶段1.7 local 合并（相邻 local 并一条；只缩短才提交）
阶段1.75 多重赋值拆分（非 local 多重赋值在安全条件下拆为单赋值序列；编码层兑现节省）
阶段1.8 变量复用（活跃区间不相交则共享名 + 省 local；SSA 等价校验 + 只缩短；跨控制流保守则回退）
   │
阶段2 编码层：去注释 + 间隔符最小化 + 单行
   │   ├─ 真·Lua 语法校验 + luaparse 校验 + AST 等价校验
   │
输出  'l ' + 结果
```

## 多段输入怎么合并、段首 `l` 怎么处理

`preprocess(input)` 的规则：

1. 把输入按行拆开（先把 `\r\n`/`\r` 归一成 `\n`）。
2. 对**每一行**，匹配并删除行首的 `^[ \t]*(?:lua|l)[ \t]+`——即"可有的前导空白 + `l` 或 `lua` + 至少一个空白"。所以 `l xxx`、`lua xxx`、`   l xxx` 都被剥成 `xxx`；`l`/`lua` 后必须跟空白才剥，避免误伤 `local` 或以 `l` 开头的标识符。
3. 剥完前缀的各行用 `\n` 重新拼接，作为**一整段** Lua 交给后续阶段。最终编码层把整段压成**一个物理行**，最后统一加**一个** `l ` 前缀输出。

字符串内部的换行（长字符串 `[[...]]`）属于语义内容，全程保留。

## 已实现的自动优化技巧

- **局部变量最短化重命名**：作用域图着色，按使用频次降序分配最短不冲突名；仅当严格更短才换；回避关键字与全局名。
- **高频全局折叠**：把多次出现、且**从不被赋值**的全局（`DoorSlot`/`RoomShape`/`Game`/`Isaac`/`pairs`…）提取成开头 `local A,B=...`，按盈亏公式决定。
- **成员字段折叠**：高频 `obj.Field`（`.Data`/`RIGHT0`/`ROOMSHAPE_1x1`…）→ `obj[v]` + 提取 `v='Field'`。`obj.F` 与 `obj["F"]` 在 Lua 中完全等价，安全。
- **仿射因子分解**：折叠出的字符串别名值若共享公共**前缀或后缀**（如 `ROOMSHAPE_*`），提取因子 `p='ROOMSHAPE_'`，其余写 `p..'1x1'`。**迭代提取多个因子**：每轮在仍是纯字符串字面量的别名值里找最优前/后缀，逐个按收益闸门提取，直到再无正收益因子；仅在总长下降时合并。
- **统一频次竞争**：局部、全局、成员三类符号在**同一张频次表**里竞争最短名（高频长全局优先拿单字母）。
- **`:method` 折叠**：`obj:M(args)` → `obj[s](obj,args)` + `s='M'`。**仅当 base 是简单标识符**（避免 `u():M()` receiver 被双求值），且**实测折叠后整体更短才提交**。
- **字段前缀折叠（阶段 1.6）**：`obj.PREFIX_X` 系列共享公共前缀时提取因子，把每处改写为 `obj[U..'rest']`。前缀候选包括 `_` 切分的边界（`ACTION_`、`ROOM_`）+ 任意两字段的最长公共前缀（覆盖 `ACTION_SHOOT` 这类不以 `_` 结尾的共享段）。因子能注入到现有 batched local 的尾部，避开独立 `local` 关键字开销。每处省 `|P|−|U|−5` 字，单字母 alias 时 `|P|≥7` 才赚。
- **字符串字面量内联（阶段 1.65）**：扫描所有标识符样的字符串字面量（`'string'`/`'table'`/`'Drop2Shoot'` 之类，`[A-Za-z_][A-Za-z0-9_]*` 长度 ≥3），同内容重复 ≥2 次时提取 `u='X'` 别名注入到 batched local，把每处 `'X'` 替换为 `u`。`canonical` 通过 `stringAliasByLocal` 把读 `u` 还原为字符串字面量，自动覆盖等价校验。**StringCallExpression 的 argument**（如 `require'X'`）显式排除，避免 `requireu` 这种合并 token 的语义破坏。
- **`local` 合并**：相邻 `local A=x local B=y` → `local A,B=x,y`，省 `local ` 关键字；要求后条不引用本组刚声明的名、组内不重名、非末条 `#init==#vars` 且不以多返回值结尾。
- **多重赋值拆分（阶段 1.75）**：非 local 多重赋值 `a,b,c=v1,v2,v3` 在安全条件下拆为 `a=v1 b=v2 c=v3` 单赋值序列。安全条件：目标全是简单 Identifier、互不重名、RHS 不读任何目标（按 binding 身份判定，避免局部 / 全局同名误判）、`#init==#vars`。每个非末值符号结尾的 `vi` 在编码层兑现 1 字节省（`)next` 紧贴而非 `,next`）。`canonical` 把符合安全条件的多目标 `Assign` 同步展开为单目标序列，等价校验自动覆盖。
- **变量复用**：活跃区间不相交（同作用域、未被闭包捕获、不在循环体、前者最后使用早于后者声明）的局部共享名字，后者 `local` 降级为赋值。
- **编码层**：去注释 + 间隔符最小化 + 压成单行。

所有折叠/合并/复用/拆分都带**"只缩短才提交"的实测长度闸门**，不赚则跳过。

## 处理细节

- `l`/`lua` 前缀按行剥离再合并单段（见上）。
- **数字紧贴字母按"畸形数字"吞成一个 token**：修了 `168do`、`1and` 这类真·Lua 会报错、而宽容解析器会放过的 bug——间隔符最小化遵循真·Lua 5.3 词法，这类边界强制保留空格（`168 do`、`1 and`），而 `)then`、`}local` 等符号收尾处贴紧。
- **绝不从 AST 反向生成代码**：luaparse 0.3.1 不在 AST 保留括号（`(f())` 与 `f()` 解析结果相同），反生成会破坏多返回值截断/优先级语义。因此所有改写都在**原始 token / 字符区间**上做，AST 只用于作用域信息与等价校验。
- **重命名只动局部变量**：全局（`Isaac`/`Game`/`ModCallbacks` 等运行时注入）一律不改，短名分配回避所有被引用的全局名，杜绝"局部被改成某全局名导致捕获"。
- **冒号方法的隐式 `self` 视作 pinned 局部**：`function M:Name()` 有一个 luaparse 不放进形参表的隐式 `self`。分析阶段为这类方法体显式登记一个**不可重命名**的 `self` 局部绑定，因此 `self` 既不会被当成自由全局而被折叠/上提到模块顶层（那样运行期 `self:m()` 会 index nil），也不会被改名破坏 `:` 语法。等价校验与重命名共用同一分析，故两侧一致。

## 验证细节（每阶段后都做，任一不过即抛错、拒绝输出）

1. **真·Lua 5.3 语法校验**：用内置 fengari 的 `load()`，与游戏内解释器一致，能抓住 luaparse 宽容放过的畸形数字等。
2. **luaparse 复核**：再建一次 AST，确认可解析。
3. **AST 语义等价（SSA 版本化）**：把"局部变量被赋值后视作新逻辑变量"做 alpha 归一深比较。这使变量复用（`local a=1;a=2` ≡ `local a=1;local b=2`）可被严格验证；并归一了 `obj.F`/`obj["F"]`、`obj:M()`/`obj[m](obj)`、`local a,b=` 与连续 `local`、`'X'` 与 `"X"`（按内容归一）、`obj[U..'X']` 与 `obj.<P+X>`（前缀因子还原）、`obj[u]` 与 `obj.X`（字符串别名还原）、安全条件下的多重赋值与单赋值序列等等价形态。SSA id 最终按首次出现顺序重编号（alpha-归一标准形），消除 SSA id 分配顺序差异。
4. **优雅回退**：变量复用跨复杂控制流时若 SSA 校验保守判负，则自动放弃复用、不影响其它阶段，**绝不输出未通过校验的代码**。

每步的「语法 / AST 等价 + 通过/失败 + 详情」记录在 `report.steps`，构造过程（每步前→后长度）记录在 `report.build`，供程序化使用。

## 测试

```
node test.js       # 作用域/遮蔽/全局保护/前缀/拒绝/真实片段
node edge.js       # 数字-关键字-运算符边界、goto、varargs、长字符串、:method
node realtest.js   # 仓库 33 个 .lua 里全部 l 段逐段压缩 + 等价复核
node remotetest.js # 拉取 remote-sources.json 列出的真实模组 main.lua（整文件）压缩 + 等价复核；首次联网后缓存到 .remote-cache/（已 gitignore），之后离线可跑，--refresh 强制重拉
node bench.js      # 正常格式代码的压缩率演示
```

当前：`test 69/69`、`edge 40/40`、`realtest 255/255`、`remotetest 4/4` 全部通过且语义等价（`realtest` 段数随仓库 .lua 文件内容浮动）。

## 已知边界

- 只做"可机器证明等价"的技巧：局部重命名、纯读取全局折叠、成员/仿射折叠、`:method` 折叠、字段前缀折叠、字符串字面量内联、多重赋值拆分、`local` 合并、变量复用（SSA 验证）、去注释、挤空格——全部带"只缩短才提交"实测闸门。
- **变量复用**只在 SSA 版本化等价能确认时应用；跨复杂控制流（分支/循环交织）保守判负则回退不复用——保证永不输出未验证代码，代价是这些位置拿不到复用收益。
- **多重赋值拆分**仅对非 local 多重赋值生效（local 多重赋值由 `local` 合并的反向负责）；目标含索引/成员（`t[i]`）或目标耦合（`a,b=b,a`）时跳过。
- 全局/`:method` 折叠**只针对从不被赋值的全局 / base 为简单标识符的方法调用**，避免改变全局状态或 receiver 双求值。
- **字段前缀折叠 / 字符串字面量内联**对 `function obj.PREFIX_X()` 名字链跳过——这些位置语法上必须保持 `name(.name)*(:name)?` 形态，不能改写为 `[alias]`。
- 字符串内部换行（长字符串 `[[...]]`）是语义内容，保留；长字符串与短字符串内容不归一（Lua 转义规则不同）。
- 第二部分里的部分人工技巧（`if not` 换分支体、跨段提全局助手等）依赖语义判断、自动化易误伤，**未纳入**自动器，仅作人工参考。

---

# 第二部分　以撒代码压缩技巧深度剖析

> 面向「玩家用 `l <代码>` 控制台命令单行执行 / 构建成 main.lua」的 Lua 代码压缩。剖析每个技巧的用法、取舍、变种、联动，并给出可计算的成本模型。

## 0. 背景与目标函数

**执行方式决定一切约束。** 玩家把每段开头带 `l ` 的整行粘进游戏控制台。`l` 是控制台命令 `lua` 的简写：控制台默认不把输入当 Lua，必须以 `l`/`lua` 开头才会执行其后的内容。所以：

- **一行 = 一条 `l <整段Lua>` 命令，控制台一次只执行一行** → 这是"一个功能必须压成单物理行"的根本原因。
- `l ` 这 2 个字符是每段无法省的固定开销。
- 打包成 `main.lua` 时由游戏直接加载、不过控制台，`clean_code()` 把行首 `l ` 剥掉。

**目标函数**：最小化「`l ` 之后、到行尾」的**总字符数**——注意，这**包含变量命名与声明部分**。任何"别名/抽取"技巧都必须把**定义代价**算进总账。

**约束次序**：① 字符数最少（主）；② 不崩（次，靠 pcall 包装 + 匿名回调清理）；③ 可读性基本不在目标内。

**作用域特性**：每个编号段是独立片段，段间不共享 `local`。所有复用只能在单段内发生。

> **核心审阅原则**：用一个技巧前，先算「定义代价 + 每次使用代价 × 次数」与「不用技巧的原始代价」对比。省不下来就是**反向压缩**。下文凡涉及别名/抽取都给出盈亏平衡点。

## 第一节：间隔符号最小化（最基础、最易误解）

Lua 词法规则：**两个 token 之间，只有当「都是 名字/关键字/数字」类、直接相邻会被误读为一个 token 时，才必须加空格**；只要有一侧是符号（`( ) [ ] + - * / % < > = ~ , .` 等），符号本身就是分隔符。

### 1.1 括号与空格等价，不存在"多余括号"

```lua
if(A)then   -- i f ( A ) t h e n = 9 字
if A then   -- i f _ A _ t h e n = 9 字
```

两者**完全等长**：`(` `)` 各占 1 字，恰好替换了 `if`后、`then`前那两个**本来就省不掉**的空格。真正要做的是：**在符号已充当分隔符的地方，绝不再加空格**。

```lua
if a and b()then   -- 正确：b() 的 ) 充当分隔符，)then 无空格
-- 错误：if a and b() then（) then 多空格）/ if(a and b())then（多包一层 +2 字）
```

### 1.2 比较式重排，让"符号收尾"的操作数贴关键字（省空格）

`0then` 会被当"畸形数字"、`athen` 会被当一个标识符，所以 `0 then`、`a then` 的空格省不掉。但让 `)`/`]` 收尾的操作数贴 `then` 就能省。利用比较可交换、可翻转方向：

```lua
-- 原 if 5%s()>0 then (15) → 改 if 0<5%s()then (14)  省1
-- 原 if b()==a then (14)  → 改 if a==b()then (13)    省1
```

规律：把以 `)`/`]`/引号结尾的操作数放在靠近后续关键字一侧；以名字/数字结尾的放远侧；需要时翻转 `<`/`>`、交换 `==`/`~=` 两侧。

### 1.3 多值赋值的"尾值符号收尾"

末值以符号结尾，则它与下一条语句的关键字/名字之间不用空格：

```lua
f,s,g=1,'Challenge',Game()   -- 末值 Game() 以 ) 收尾 → ...Game()c=g[s] 无需空格
```

做多值赋值时主动把"以 `)`/`]`/`'...'`结尾"的值排到最后（如 `S[a],F(),'sst'`），再省一个空格。

### 1.4 能省的调用括号一律省

```lua
require'debug'   RegisterMod'x'   pairs{a,b}   SoundEffect['SOUND_'..v]
```

## 第二节：变量折叠（别名）的成本核算

### 2.1 折叠盈亏平衡——引用 < 2 次绝不折叠

总长度含声明，给只用一次的东西起别名一定只增不减。

- 函数别名（`G=Game`，约 +6 字定义；`Game()`=6 字、`G()`=3 字）：`6+3k < 6k → k≥3` 才划算。
- 别名"调用结果"（`local G=Game()` 复用对象）：盈亏点降到 **k≥2**。

按回调数 N 的临界写法：

| 回调数 N | 写法 | 总字数 |
|---|---|---|
| N=1 | 直接 `Isaac.AddCallback({},...)` | 35 |
| 1<N≤4 | `local A,M=Isaac.AddCallback,ModCallbacks` | 41+8N |
| 4≤N≤10 | 再共享一张表 `local A,M,T={}` | 45+7N |
| N≥10 | 包成函数 `local M,A=...function(...)Isaac.AddCallback({},...)end` | 65+5N |

### 2.2 方法名抽公共字符串的"动态核算"（最易反向压缩）

`x:Name()` 变成 `x[s](x)` / `x['Get'..B](x)`。设方法名长 `m`、调用 `k` 次、别名单字母：

```
直接 x:Name()  = k(m+4)
抽取 x[s](x)   = (m+5)+7k
抽取-直接 = m+5 + k(3-m)
```

令其 <0：`m=4`（`Kill`）需 k≥10（几乎不可能，纯亏）；`m=9`（`GetSprite`）需 k≥3；`m≥14` 需 k≥2。**短名抽取必亏。**

两个"必抽"场景（为逻辑而非省字）：① 方法名需动态拼接（`p['Get'..B]` 跨 Get/Add/Has/Remove 共享，声明成本摊薄）；② 同名方法在不同对象/分支间切换。

### 2.3 折叠位置（在哪个作用域声明）

- 多作用域都用 → 在共同(外层)作用域声明一次。
- 单作用域用 → 通常也并进外层那条批量 local（搭顺风车不额外花 `local`）。
- 单字母用尽时 → 才把单作用域变量放回它自己的作用域。
- 始终优先想办法省掉 `local`（最典型：在参数列表里声明，见 3.2）。

## 第三节：声明与赋值技巧

### 3.1 前向声明（占位 + 延迟赋值）实现互递归

```lua
local D,E,F,...,A,B,C=require'debug',{},'Function',...        -- 第一批：登记
_CBH,A,B,C=Y,D.getlocal,...,function(i)...end,function(f)...end -- 第二批：填真值并互引
```

让闭包把同级局部当 upvalue 捕获、实现互相引用。这是满屏"值的数量不够被赋 nil"警告的来源——**故意为之**。

#### 3.1a 因子声明能否前向合一？字符级精算

考虑这种结构（来自仿射因子分解）：
```lua
local az='ROOMSHAPE_' local f,...,D,...,O=RoomShape,...,az..'1x1',...,az..'LBR'
```

直觉上想合一成一条 local 用前向声明节省一个 `local ` 关键字：
```lua
local f,...,az,D,...,O=RoomShape,...,'ROOMSHAPE_'   -- D~O 自动 nil
D=az..'1x1' E=az..'IH' ... O=az..'LBR'
```

但实测发现这反而**变长**。设因子被派生引用 K 次、单字母派生名长度为 1，字符级差账：

- 省去独立 `local az='F' `：约 |F|+11 字
- `,az` 进 names + `,'F'` 进 values：|F|+5 字
- 删除 K 处 `,az..'X'`，加 K 条独立赋值 `D=az..'X' `（含段间分隔）：每处净增约 2 字

**B − A = 2K − 6**

| K | B − A | 结论 |
|---|---|---|
| 1 | −4 | 合一更短 4 字 |
| 2 | −2 | 合一更短 2 字 |
| 3 | 0 | 打平 |
| 4 | +2 | 独立更短 2 字 |
| 12 | +18 | 独立更短 18 字 |

**临界点 K=3**。但 `buildDeclParts` 的因子分解门槛本就要求多次重复才有正收益（典型 K≥4），所以**因子分解能触发的场景，独立 local 几乎总是最优**。前向合一仅在 K=1 或 K=2 时省字，而那种数量通常根本不会触发因子提取——两个机制覆盖的区间几乎不交叠。

3.1 真正的用武之地是**结构上必须**前向声明的场景（互递归、闭包 upvalue 共捕获），不是为了省 `local ` 关键字。

### 3.2 在参数列表里声明（省 local）

```lua
function(s,p,g,c,f)f,s,g=1,'Challenge',Game()...  -- 5 个形参里只有 p 是真参，其余全是免费局部
```

代价：形参语义被破坏，但在"一段一抛"里可接受。是省 `local` 的主力。

### 3.3 多值赋值赋 nil（省字 / 复位）

```lua
local a,b,c=1,2   -- c 自动 nil
a,b=nil           -- b 复位，比 a,b=nil,nil 短
```

### 3.4 字符串常量化成员名 + 词根二次派生

```lua
B='Collectible'  p['Get'..B](p,i)  p['Add'..B](...)  C[B..'s'](C)
P='Position'     O=P..'Offset'     e[P]  e[O]
```

### 3.5 布尔 / 哨兵常量别名

```lua
Y=true ... return Y      -- true(4)→Y(1)，≥2 次回本
local N ... b=N          -- N 始终 nil 当 false 哨兵（仅纯布尔判定，勿取长度/索引）
```

## 第四节：控制流与表达式技巧

### 4.1 `if not` 二择 → 去 not + 交换分支体

```lua
-- 原 if not c then A else B end → 改 if c then B else A end  省 not(4字含空格)
```

单分支 `if not f then return end` 不能套（无 else）。

### 4.2 `pairs` 优先于 `ipairs`（效果无关时）

顺序无关时用 `pairs`(5) 省 1 字。

### 4.3 `and`/`or` 三元 + `table.unpack` 归一

```lua
c,n=table.unpack(type(i)=='table'and i or{i,1})   -- {id} 与 {id,n} 统一解包；a 不能为 false/nil
```

### 4.4 位运算 / 取模替代多重比较

```lua
~4&c   0<f&(D.A|D.B)   e[I]%#C+1   i|G==s|G
```

科学计数法省字：`9e9`/`1e3`/`1e-3`/`1<<37`。

## 第五节：变量复用（单段内）

| 技巧 | 写法 | 要点 |
|---|---|---|
| 形参当 scratch | `function(a,p,b)b='Parent'a=p[b]...p[b]=a` | 省 local |
| 一名多义 | `function(p,f,v,s)...p,f=Game():GetItemPool()` | 前一用途须已结束 |
| 字符串双义 | `P='Position' O=P..'Offset'` | 既取值又当词根 |
| 方法名变量化 | `Re='Remove' e[Re](e)` | 仅当复用≥2 次且过 2.2 核算 |
| 多重赋值交换 | `N[i],N[p]=N[p],N[i]`；`G,E,O=c,r,..(r-E)` | 免临时变量；右侧先求值 |
| `_` 多场景 | `function(_,p)`；`for _=1,n do`；`_,i=A(3,i)` | 同名承担所有"不关心" |
| 句柄表合一 | `T={M={}}` 同时当 mod 句柄 + 数据表 `T[hash]` | 键空间不冲突即安全 |

## 第六节：命名空间表（绕过局部上限 + 闭包只捕获一个 upvalue）

```lua
local D={B={},D={},T={},R=F,C=F,G=F,M={},W={}}   -- 状态全挂字段
```

三重收益：① 绕过局部变量/upvalue 数量上限；② 多闭包只捕获 `D` 一个 upvalue；③ 整表快照一行搞定（`D.B=C(D.T)` 深拷贝）。代价：`D.R` 比裸局部多 2 字。"多状态 + 多闭包"专用，单状态反亏。

## 第七节：健壮性"重型"基础设施

- **安全包装**：`debug.sethook` 拦截 `AddCallback`，把每个回调包进 `pcall`；`O`(原→包装)、`W`(包装→原) 两表互查防重复包装；`if not(REPENTOGON or _CBH)` 在 Repentogon 环境跳过。
- **清理匿名回调**（`CLM()`）：删掉无 `Mod.Name` 的匿名注册，防反复粘贴污染。
- **数据持久化**：命名空间表 + 深拷贝快照，兼容发光沙漏/rewind。

## 第八节：速查决策表 + 审阅口诀

| 想做 | 用技巧 | 回本/适用条件 |
|---|---|---|
| 消除空格 | 1.1/1.2/1.3 间隔符最小化 | 总是；比较式重排让 `)`收尾贴关键字 |
| 省调用括号 | 1.4 | 单字符串/表实参 |
| 对象/枚举别名 | 2.1 | 函数别名 k≥3；结果别名 k≥2 |
| 方法名抽字符串 | 2.2 | `m+5+k(3-m)<0`；或动态拼名必抽 |
| 省 local | 3.2 参数列表声明 | 总是优先 |
| 互递归闭包 | 3.1 前向声明 | 需互引时必用 |
| 廉价 nil | 3.3 多值赋值 | 复位/前向声明 |
| 多状态+多闭包 | 6 命名空间表 | 状态≥4 或 闭包≥3 |
| 二择分支 | 4.1 去 not 换体 | 有 else 时 |
| 遍历 | 4.2 pairs | 顺序无关 |

**通用口诀**：
1. 每个空格都问"能不能靠符号收尾消掉"（重排比较式、调整赋值尾值）。
2. 每个别名都问"它被用了几次、省的够不够付声明"——`l` 后含命名总长，少于盈亏点就是反向压缩。
3. 每个方法名变量都跑 `m+5+k(3-m)<0`，短名少调一律不抽。

## 附录：linter 报错的统一解释（均不影响游戏内运行）

- `该表达式不能作为语句`：行首 `l ` 是控制台 `lua` 命令前缀，非合法 Lua 语法；打包时 `clean_code()` 剥掉。
- `由于值的数量不够被赋值为 nil`：前向声明/多值赋值故意为之。
- `未定义的全局变量 Isaac/Game/...`：游戏运行时注入的全局，静态分析看不到。
- `重定义局部变量`：段与段独立，同名 `local` 互不影响。
