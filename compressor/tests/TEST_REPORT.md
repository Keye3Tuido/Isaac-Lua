# LuaMin 测试报告

> 生成时间: 2026-06-01
> 范围: 透明别名消解（#1+#2）+ 阶段1.10 安全闸门修复 + 智能声明合并（#3：canonical 死前向声明归一 + 阶段1.7b 声明上提）+ 分号消除 + relocation float + 结构重构

## 压缩结果基线快照（重构前后字节对照基准）

`tests/snapshot.js` 把所有可用 Lua 输入（手写单测用例 + 仓库 33 个 .lua 的逐段/合并 + 远程缓存模组 + bulk 开源仓库）逐个压缩，记录 `输入长度 / 是否成功 / 输出长度 / 输出 sha`，写入 `tests/_snapshot.json`。`--check` 模式与基线 `tests/_snapshot_baseline.json` 比对，**输出变长或由可压缩变为报错即判 FAIL**。这是重构的安全网。

**基线（重构前）：898 用例，892 成功，6 报错（均为输入文件自身非标准 Lua），总输出 1,859,021 字节。**

分类小计：

| 语料 | 用例 | 成功 | 输入字节 | 输出字节 |
|---|---|---|---|---|
| unit（手写关键用例） | 4 | 4 | 697 | 530 |
| repo-seg（仓库逐段） | 255 | 255 | 125,488 | 124,224 |
| repo-merged（仓库按文件合并） | 33 | 33 | 125,710 | 118,592 |
| remote（远程模组整文件） | 4 | 4 | 62,252 | 28,040 |
| bulk（开源仓库整文件） | 602 | 596 | 2,863,518 | 1,587,635 |

关键单测用例输出长度（重构后必须 ≤ 此值）：

| 用例 | 输入 | 输出 | 压缩率 |
|---|---|---|---|
| multi_callback | 265 | **176** | 33.58% |
| callback_pattern | 217 | 145 | 33.18% |
| field_repeat | 106 | 100 | 5.66% |
| single_alias | 109 | 109 | 0%（已最优） |

> 重构准则：`node tests/snapshot.js --check` 必须 `RESULT: OK`（regress=0、lost-ok=0）。

## 结构重构（2026-06：core.js 拆分为 src/ 模块）

`core.js`（原 2773 行单文件）按职责拆分，便于维护：`core.js`（231 行）保留词法器与 `create()` 工厂；逻辑分到 `src/analyze.js`(251) / `plan.js`(427) / `encode.js`(89) / `canonical.js`(661) / `folds.js`(966) / `compress.js`(231)。各模块为"安装器"——`create()` 构造共享上下文 `C`，依序运行各安装器把函数挂到 `C`，最后导出 API。函数体逐字保留（仅加导入/导出包裹），故行为不变。

**重构验证**：`snapshot.js --check` 全 898 用例字节级 `RESULT: OK`（regress=0、lost-ok=0）；全部测试套件通过；浏览器加载路径（`index.html` 依序 `<script>` 引入 `src/*.js` + `core.js`，无 require）经模拟验证产出一致（multi_callback=176，与基线相同）。



## 测试套件概览

| 套件 | 命令 | 用例数 | 通过 | 说明 |
|------|------|--------|------|------|
| unit       | `node tests/test.js`                  | 69  | 69  | 作用域/遮蔽/全局保护/前缀/拒绝/真实片段 |
| edge       | `node tests/edge.js`                  | 40  | 40  | 数字-关键字-运算符边界、goto、varargs、:method |
| incr       | `node tests/test_incremental.js`      | 3   | 3   | 增量压缩测试（单条 vs 合并） |
| elision    | `node tests/test_transparent_elision.js` | 5 | 5 | 透明别名消解专项（多回调/单别名/三相同/别名链/重新赋值） |
| fwdnil     | `node tests/test_canonical_fwdnil.js` | 11  | 11  | canonical 死前向声明归一（5 正例 + 6 负例：区间读 v / 闭包捕获 / 自引用等） |
| real(逐段) | `node tests/realtest.js`              | 255 | 255 | Isaac 项目 33 个 .lua 内全部 `l` 段逐段压缩 + 等价复核 |
| real(合并) | `node tests/realtest.js`              | 33  | 33  | Isaac 项目按文件合并全段压缩 + 等价复核 |
| remote     | `node tests/remotetest.js`            | 4   | 4   | 远程模组整文件压缩 + 等价复核（缓存于 .remote-cache/） |
| **合计**   |                                       | **420** | **420** | **100%** |

所有用例均通过真·Lua 5.3 `load()` 语法校验 + luaparse 复核 + SSA 版本化 AST 等价校验。

## 本轮主要改动

### 1. 透明别名消解（阶段1.1，对应待办 #1 变量值追踪 + #2 别名替换）

在 `planAll`（结构规划阶段）实现 copy-propagation：识别**只读、从不被赋值、init 溯源到从不被赋值全局**的局部别名（含 `local g=Global; local h=g` 的链式溯源到不动点），把对它的所有使用重定向到全局折叠别名（`M.XXX` → `b.XXX`），并删除该别名的声明项。

- **安全判定**：按名归并（同名 binding 必须全部是同一全局的透明别名，规则 R1）+ 单声明单别名、位置对齐（规则 R2）。
- **等价校验（非旁路）**：`canonical` 内置"透明别名归一"，把只读别名读还原为源全局读、删除其声明；该归一对压缩前/后**双侧自动一致施加**（无需传别名映射），因此 `realtest`/`remotetest`/`bulktest`（原始侧不传映射）也严格收敛。移除了旧版"有透明别名就跳过语义校验"的危险旁路。
- **只缩短才提交**：`compress` 跑启用/禁用消解两条流水线取更短者（消解在"多条完全相同声明"等形态下反更长，退回阶段1.10 去重）。
- **效果**：`test_multi_callback.js` 204 → 186 字符（压缩率 23.02% → 29.81%）。

### 2. 阶段1.10 重复声明删除：补安全闸门（修复既有正确性 bug）

阶段1.10 是正则后处理，运行在最后一次 AST 等价校验**之后**，canonical 无法建模其语义。修复前它只有"只缩短"长度判断，缺少语法校验，会在跨语句误删时输出 **broken Lua**（`compress` 仍报成功）。本轮加入：删除后真·Lua `load()` 语法复核 + luaparse 复核，任一不满足回退到删除前快照；并修正了回退路径（旧版 `slice(0,beforeDedup)` 会产出残缺代码）。

- 此修复消除了 8 个 bulktest 输出级语法错误（这些文件在改动前 `compress` 报成功但输出不可解析）。

### 3. 智能声明合并（#3：canonical 死前向声明归一 + 阶段1.7b 声明上提）

分两步落地：

- **canonical 死前向声明归一（奠基）**：识别 `local v=nil`（或缺省 init）后、`v` 在到达其同块首次赋值前从不被读（含闭包捕获、嵌套块读取）的"死前向声明"。归一时删除该 nil 声明且不 bumpDef，使首次赋值成为 v0——于是 `local v=nil ...(不读v)... v=e` 与 `...(不读v)... local v=e` 收敛同形。健全性前提 P1–P5；支持多目标首次赋值（经 `multiAssignSafeToSplit`），避免与多重赋值拆分阶段不对称。新增 `tests/test_canonical_fwdnil.js`（5 正例 + 6 负例）。
- **阶段 1.7b 声明上提**：把顶层块内、别名头之后声明的局部变量上提到别名头作前向 nil 占位，并降级其 `local`；多变量降级为多重赋值后再跑一次多赋值拆分。三关闸门：只缩短 + 真·Lua 语法 + canonical 等价。

效果：多回调用例 186 → 177；bulktest 全量**净省 1788 字节**且零回归。

**sound 性边界（177 → 169 暂不实现）**：理想 169 需对索引表达式 `a[c]` 做冗余存储消除，但 `a[c]` 可能触发 `__index` 副作用、静态不可证纯，不属于可机器证明等价的变换。`canonical(177 形)===canonical(173 形)` 返回 false（归一正确拒绝判等）。本压缩器的设计红线是"只做可机器证明等价的变换"，故不追求该 8 字。

## 批量开源项目测试（bulktest）

自动克隆约 20 个知名 Lua 开源项目（个别仓库归档/无 .lua 文件跳过），**609 个 .lua 文件 (3.7MB)** 全量压缩 + 输出语法验证。

| 指标 | 数值 |
|------|------|
| 文件总数 | 609 |
| 通过 | 596 |
| 失败 | 6 |
| 通过率 | 97.9% |

### 失败明细（6 条，均非压缩器正确性问题）

| 文件 | 大小 | 失败阶段 | 原因 |
|------|------|----------|------|
| penlight/lua/pl/config.lua | 6944B | 输入校验 | 原始文件非标准 Lua（第80行 `=` 处语法错误） |
| busted/busted/core.lua | 9507B | 阶段1.1/语法 | 结构阶段已知边界（baseline d0c350f 即失败，与本轮改动无关） |
| jumper/jumper/core/lookuptable.lua | 897B | 输入校验 | 原始文件函数声明语法错误 |
| jumper/specs/pathfinder_specs.lua | 10326B | 输入校验 | busted 测试框架 DSL，非独立可 load 的 Lua |
| zerobrane/lualibs/sha2.lua | 6531B | 输入校验 | 原始文件 Lua 5.2 专有语法 |
| lapis/lapis/cmd/templates/spec.lua | 3865B | 输入校验 | busted 测试框架 DSL |

> 5 条为输入文件自身非标准 Lua（压缩器正确拒绝）；1 条（busted/core.lua）为结构阶段既有边界，与本轮改动无关。

### 回归核验（三方对比）

对同一份缓存仓库分别用三个版本的 `core.js` 跑 bulktest（602 个有效文件子集口径）：

| 版本 | 通过 | 失败 |
|------|------|------|
| baseline `d0c350f` | 596 | 6 |
| committed `HEAD`（含先前 stage1.10/别名链提交） | 588 | 14 |
| 本轮工作树 | 596 | 6 |

- 本轮相对 committed HEAD：**0 新增失败，修复 8 个**（先前 stage1.10 引入的输出级 broken 代码）。
- 本轮相对 baseline `d0c350f`：失败集**完全一致**（零回归），同时新增透明别名消解收益。

## 压缩率（realtest，Isaac 项目）

逐段 255 段 `l` 代码：输入 124,978 → 输出 123,718（省 1,260 字符，约 1%）。

## 多回调场景（test_multi_callback.js）

| 阶段 | 字符数 | 压缩率 |
|------|--------|--------|
| 原始输入 | 265 | — |
| 改动前压缩 | 204 | 23.02% |
| 透明别名消解（#1+#2） | 186 | 29.81% |
| 声明上提（#3，阶段1.7b） | 177 | 33.21% |
| 理想目标（受 sound 性边界阻挡，不追求） | 169 | 36.23% |

177 是"只做可机器证明等价变换"原则下的安全最优；剩余 8 字需对索引表达式做冗余存储消除（`__index` 可能有副作用，不 sound）。
